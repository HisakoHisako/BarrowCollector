# Discord incoming webhook URL
# (Create in your Discord server: Channel Settings -> Integrations -> Webhooks)
DISCORD_WEBHOOK_LOCAL=http://localhost:8787/webhook/local
DISCORD_WEBHOOK_GLOBAL=http://localhost:8787/webhook/global

# Optional: where scripts send requests
BARROW_BASE_URL=http://localhost:8000

# Optional: pulse interval (ms)
PULSE_INTERVAL_MS=15000
